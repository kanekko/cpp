#include <iostream>

int main()
{
    int total = 10;
    // Suma
    // total = total + 1;
    //total += 1;

    // total = total + 2;
    //total += 2;

    // total++; // operador de incremento

    // Resta 
    // total = total - 1;
    // total -= 2;

    // total--; // operador de decremento

    // Multiplciación
    // total = total * 2;
    // total *= 2;

    // División
    // total = total / 3;
    // double totalDouble = 10;
    // totalDouble = totalDouble / 3; 

    // std::cout << total << std::endl;
    // std::cout << totalDouble << std::endl;

    // Módulo
    // int remanente = total % 3;
    // std::cout << remanente << std::endl;

    // int esPar = total % 2;
    // std::cout << "par: " << esPar << std::endl;

    // Orden de presencia 
    // 1. Parentesis
    // 2. * y /
    // 3. + y -
    int resultado = 2 + 3 - 1 * 8 / 2;
    // int resultado = 2 + 4 - ((3 * 8 )/ 2);
    // int resultado = (2 + (4 - (X)));

    std::cout << resultado << std::endl;

    // return 0;
}